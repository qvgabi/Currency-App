<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gabriela_matuszewska_sr_12_30_projekt2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gabriela_Matuszewska_sr_12_30_projekt2"
        tools:targetApi="n">
        <activity android:name=".CalculatorCurrencyActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".GoldActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".HistoricRatesActivity"
            android:parentActivityName=".CurrencyActivity" />
        <activity
            android:name=".CurrencyActivity"
            android:parentActivityName=".MenuActivity" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>